# MyBatis
# 是否启用日志
log: true
# 二级缓存相关
cache:
  # 是否启用
  enabled: true
  # 过期时长（秒）
  expired: 43200
  # 缓存属性（具体有哪些属性由 CacheFactory 的服务提供方决定）
  properties: {}
# MyBatis 属性
properties:
  # 自动驼峰命名转换
  mapUnderscoreToCamelCase: true